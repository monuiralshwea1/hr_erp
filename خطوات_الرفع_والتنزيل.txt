════════════════════════════════════════════════════════════════════
  خطوات الرفع إلى GitHub والتنزيل على سيرفرات أخرى
  مشروع hr_erp — شركة الشاحذي
  السيرفر: newsmart@newsmart  |  البينش: ~/frappe-bench2
════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ 1) الرفع من التطبيق (السيرفر المحلي) إلى GitHub                  │
└─────────────────────────────────────────────────────────────────┘

اتصل بالسيرفر المحلي ثم:

    ssh newsmart@newsmart

    cd ~/frappe-bench2/apps/hr_erp

    # مراجعة الجاهزية:
    git status
    git log --oneline -5

    # الدفع إلى الفرع الرئيسي main:
    git push origin master:main

    ⚠ عند الطلب سيظهر:
        Username for 'https://github.com': monuiralshwea1
        Password for 'https://...':  ← الصق هنا Personal Access Token
                                       (وليس كلمة سر حساب GitHub)

كيف أحصل على Personal Access Token؟ (مرة واحدة فقط)
    1. ادخل github.com بحسابك
    2. اضغط صورتك → Settings → Developer settings
       (الرابط المباشر: https://github.com/settings/tokens)
    3. Personal access tokens → Tokens (classic) → Generate new token
    4. الاسم أي شيء، الصلاحيات: علّم على repo فقط
    5. Generate → انسخ التوكن فوراً (لن يظهر مرة أخرى)

أمر بديل يحفظ التوكن فلا يسألك كل مرة (اختياري):

    cd ~/frappe-bench2/apps/hr_erp
    git config credential.helper store
    git push origin master:main
    (أدخل المستخدم والتوكن مرة واحدة وسيُحفظ في ~/.git-credentials)

ما الذي يُرفع حالياً؟ (جاهز في المستودع على السيرفر)
    - تحديث مساحة عمل بوابة الموارد البشرية + مولّد البيانات
    - ضريبة الدخل 15% + تقرير كشف الرواتب الكامل + أدلة النشر
    - هذا الملف

┌─────────────────────────────────────────────────────────────────┐
│ 2) تنزيل التحديثات على سيرفر آخر — التطبيق مثبت مسبقاً            │
└─────────────────────────────────────────────────────────────────┘

على السيرفر الآخر (عدّل اسم المستخدم والمسار حسب سيرفرك):

    cd ~/frappe-bench2/apps/hr_erp

    # إن كانت هناك تعديلات محلية لا تريد فقدانها:
    git stash

    # تنزيل آخر نسخة:
    git fetch origin
    git reset --hard origin/main

    # تطبيق التغييرات على قاعدة البيانات:
    cd ~/frappe-bench2
    bench --site site1.local migrate
    bench --site site1.local clear-cache

    # إعادة التشغيل:
    bench restart
    # أو إذا تستخدم supervisor:
    sudo supervisorctl restart all

┌─────────────────────────────────────────────────────────────────┐
│ 3) سيرفر جديد تماماً — تنزيل التطبيق وتشغيل البيانات               │
└─────────────────────────────────────────────────────────────────┘

المتطلبات: Frappe v15 + ERPNext v15 + HRMS v15 مثبتة عبر bench

    # اتصل بالسيرفر:
    ssh <user>@<server>

    cd ~/frappe-bench2

    # تنزيل التطبيق من GitHub:
    bench get-app https://github.com/monuiralshwea1/hr_erp.git

    # تركيبه على الموقع (إن لم يكن مكتباً):
    bench --site site1.local install-app hr_erp
    bench --site site1.local migrate

    # تشغيل بيانات شركة الشاحذي (45 موظف + رواتب + حضور + 344 كشف):
    env/bin/python apps/hr_erp/shahadhi_full_seed.py site1.local

    ⚠ نفّذ الأمر الأخير من داخل مجلد البينش نفسه (~/frappe-bench2)
    ⚠ استبدل site1.local باسم موقعك المستهدف
    ⚠ مدة التنفيذ 4-5 دقائق — ينشئ الشركة والحسابات أيضاً إن لم توجد

النتيجة المتوقعة نهاية التشغيل:
    Employee: 45 | SSA: 45 | Shift Type: 10 | Shift Assignment: 45
    Attendance: 440 | Payroll Entry: 8 | Salary Slip: 344
    إجمالي صافي المرتبات: 36,382,663 YER (بعد خصم الضريبة 15%)

┌─────────────────────────────────────────────────────────────────┐
│ 4) ملفات مهمة في جذر المستودع                                     │
└─────────────────────────────────────────────────────────────────┘

    shahadhi_full_seed.py         السكربت الشامل (كل البيانات مدمجة)
    shahadhi_full_report.xlsx     التقرير الكامل (10 أوراق تشمل كشف
                                  الراتب بالاستحقاقات والضريبة
                                  والخصومات والصافي)
    تعليمات_التنفيذ.txt            تفاصيل الضريبة وطريقة التشغيل
    خطوات_الرفع_والتنزيل.txt        هذا الملف

مسارات سريعة على السيرفر:
    التطبيق : ~/frappe-bench2/apps/hr_erp
    الموقع  : ~/frappe-bench2/sites/site1.local
    اللوجات : ~/frappe-bench2/logs

════════════════════════════════════════════════════════════════════
